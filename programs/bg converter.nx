INPUT "#";N$
PRINT "CONVERTING..."
LOAD "#"+N$,$A000
W=PEEK($A000)
H=PEEK($A001)
FOR Y=0 TO MIN(31,H-1)
  FOR X=0 TO MIN(31,W-1)
    D=((Y*32)+X)*2
    S=(Y*W+X)*2
    POKE $D000+D,PEEK($A002+S)
    POKE $D001+D,PEEK($A003+S)
  NEXT X
NEXT Y
SAVE "#"+N$+":MAIN BG",$D000,32*H*2
PRINT "SAVED"
