REM LITTLE TEST PROGRAM
PRINT "HELLO"
PRINT 1+3+2+5+4

A=1
B=0
C=0
D=10

IF D=1 THEN PRINT "SINGLE D=1" ELSE IF D<1 THEN PRINT "SINGLE D<1" ELSE PRINT "SINGLE D>1"

PRINT "----"

IF A=1 THEN
    IF B=1 THEN
        PRINT "A=1 AND B=1"
    ELSE
        PRINT "A=1"
        IF D=1 THEN PRINT "SINGLE D=1" ELSE IF D<1 THEN PRINT "SINGLE D<1" ELSE PRINT "SINGLE D>1"
    END IF
ELSE IF C=1 THEN
    PRINT "C=1"
ELSE
    PRINT "A=0"
END IF

PRINT "----"

IF A=1 THEN
    PRINT "A=1"
    PRINT "--"
ELSE IF B=1 THEN
    PRINT "B=1"
    PRINT "--"
ELSE IF C=1 THEN
    PRINT "C=1"
    PRINT "--"
ELSE IF D=1 THEN
    PRINT "D=1"
    PRINT "--"
ELSE
    PRINT "NO"
END IF

PRINT "END"
END
